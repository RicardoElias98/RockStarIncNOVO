package Visual;

import javax.swing.*;
import java.awt.*;

public class PainelMinhasPlaylists extends JPanel {

    public PainelMinhasPlaylists() {
        initComponents();
    }

    private void initComponents() {
        setLayout(new FlowLayout());
        setBackground(Color.ORANGE);
        setVisible(false);
    }
}

