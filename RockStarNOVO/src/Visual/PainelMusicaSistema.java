package Visual;

import javax.swing.*;
import java.awt.*;

public class PainelMusicaSistema extends JPanel {
    public PainelMusicaSistema() {
        initComponents();
    }

    private void initComponents() {
        setLayout(new FlowLayout());
        setBackground(Color.ORANGE);
        setVisible(false);
    }
}
